@page "/setup-pin"
@using JournalApp.Services
@inject AuthService Auth
@inject NavigationManager Nav

<JournalApp.Components.Pages.Login />

@code {
    // Both Registration and Login are now handled by the unified Login component
}
